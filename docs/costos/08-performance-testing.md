# Costos y benchmarks: performance testing

**Estado:** draft

No hay benchmark artificial del modelo. El costo relevante depende del flujo,
entorno y carga reales. `cargo bench --all-targets` verifica la ruta del
repositorio.
