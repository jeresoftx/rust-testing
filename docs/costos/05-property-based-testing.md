# Costos y benchmarks: property-based testing

**Estado:** draft

No se agrega un benchmark artificial. El costo relevante depende de un
generador real, el tamaño del dominio y el shrinking de un sistema concreto.
Medir el modelo educativo no respondería esa pregunta. La ruta se verifica con
`cargo bench --all-targets`.
