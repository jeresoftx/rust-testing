# Costos y benchmarks: mutation testing

**Estado:** draft

No hay benchmark propio. El costo real depende de compilar y ejecutar una suite
contra muchos mutantes; este modelo no representa ese trabajo. La ruta se
verifica con `cargo bench --all-targets`.
