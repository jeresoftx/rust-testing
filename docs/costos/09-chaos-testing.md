# Costos y benchmarks: chaos testing

**Estado:** draft

No hay benchmark propio. El costo relevante es el control del entorno y la
observación de recuperación; medir este modelo no representa un experimento
real. `cargo bench --all-targets` se mantiene como verificación de ruta.
